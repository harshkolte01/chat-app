# Version Bump 0.2.1

Updated the desktop app release version from `0.2.0` to `0.2.1` after the latest post-release fixes.

Files updated:

- `package.json`
- `package-lock.json`

Reason:

- patch-level release for follow-up fixes after the `0.2.0` desktop calling release
